#include "main.h"

/**
* factorial - returns the factorial of a given number
* @n: a given number
* Return: a factorial in integer
*/

int factorial(int n)
{
	
	int fact;
	
	fact = 0;
	if (n = 1)
	{
		return(fact)
	}
	
	fact += factorial(n - 1);
	
	return (fact);
	
}
